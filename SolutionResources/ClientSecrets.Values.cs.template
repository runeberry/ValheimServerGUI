// Template for the gitignored ClientSecrets.Values.cs. Only the VALUES are secret, so this
// structure is tracked. The release workflow (.github/workflows/release.yml) fills the
// __PLACEHOLDER__ tokens from repo secrets to produce ClientSecrets.Values.cs during a CI build.
// For local development you supply your own ClientSecrets.Values.cs (see SolutionResources/README.md);
// this template is not used locally and is never compiled.
namespace ValheimServerGUI.Properties
{
    public static partial class ClientSecrets
    {
        static ClientSecrets()
        {
            RuneberryApiKeyHeader = @"__RUNEBERRY_API_KEY_HEADER__";
            RuneberryClientApiKey = @"__RUNEBERRY_CLIENT_API_KEY__";
        }
    }
}
