{
  "name": "vsg-name-lookup-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that resolves platform IDs to display names for ValheimServerGUI (replaces the retired Runeberry AWS API), with KV caching.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "wrangler": "^3.78.0"
  }
}
